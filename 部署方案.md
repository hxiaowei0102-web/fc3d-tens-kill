# 十位杀一码 · 纯云端全自动更新部署方案

> 方案日期：2026-08-21 ｜ 本地项目：`D:\十位杀一码\` ｜ 云端镜像：`D:\十位杀一码\cloud_deploy\`
> 版本：v2（2026-08-21 风险排查修复版：单入口脚本 + fingerprint 统一 + 部署兜底 + 单源兜底）

## 1. 总览

把「十位杀一码」从本地手动更新升级为**纯云端全自动**：手机打开网页即看最新预测，无需电脑、无需手动跑任何脚本。

```
GitHub Actions (三重 cron)
   ↓ 多源降级抓取开奖（跨源校验 + 单源兜底）
   ↓ 追加到 data/fc3d-history.csv（防倒灌防污染）
   ↓ 指纹变化? → 穷举5924万 → 网格扫描 → 500/1000期回测 → 下期预测
   ↓ 生成 index.html
   ↓ git commit + push（数据变化才提交）
   ↓ GitHub Pages 自动部署（每次 cron 都部署，失败自动补部署）
   ↓
手机访问 https://hxiaowei0102-web.github.io/fc3d-tens-kill/
```

## 2. 调度（cron 三重备份）

| # | UTC | 北京 | 说明 |
|---|---|---|---|
| 1 | `0 14 * * *` | 22:00 | 开奖(21:15)后首抓，覆盖当日新号 |
| 2 | `30 15 * * *` | 23:30 | 备份1：首抓失败或源延迟 |
| 3 | `0 17 * * *` | 次日01:00 | 备份2：前两轮都失败（留足时间） |

3 次机会兜底；`concurrency` 保证撞车排队不并发写 CSV。

## 3. 数据源（多源降级链）

| 源 | 类型 | 状态 | 说明 |
|---|---|---|---|
| huiniao | JSON | ✅ 主源 | 含 next_code，最快最稳 |
| 17500 | TXT | ✅ 备源 | 全量文本，跟随重定向 |
| apihz | JSON | ⚠️ 间歇限流 | code=400 频发，仅作碰运气备源 |
| cwl | JSON | ❌ 403 | 官方源 WAF 拦截（云端可能偶尔放行） |
| kjh_55128 / 55128 / 8200 / vipkj | HTML | ❌ 失效 | 保留占位，降级链自动跳过 |

**防污染（三层）**：
1. **跨源交叉校验**：新期号 ≥2 源一致才采纳多数票；同号冲突有多数票 → 采纳多数并告警
2. **单源兜底**：多源全部失效只剩 1 个源时，若该期号 = 本地最新+1（紧邻期）→ 采纳并告警，避免开奖永远无法入库卡死更新
3. **防倒灌**：CSV 只追加严格晚于本地最新期号的期号，伪造旧期/乱序数据直接丢弃

## 4. 重算逻辑（update.py 单入口）

`update.py` 是本地/云端**共用单入口**，同一份代码两处跑：

| 模式 | 命令 | 输出 |
|---|---|---|
| 本地 | `python update.py` | `十位杀一码.html` |
| 云端 | `python update.py --cloud` | `index.html`（Pages 入口） |
| 强制重算 | `python update.py --force` | 忽略缓存全量重算 |

- **数据未变**：跳过穷举/回测（省 Actions 分钟），仅复用缓存生成页面（幂等）
- **数据已变**：穷举 5924万(≈443s) → 网格 270 组合 → 回测 → 预测 → 生成页面
- **缓存缺失**（首次部署）：强制全量重算，不崩
- 总时长：无变化 ~40s，有变化 ~10min（远小于 60min 超时上限）

### fingerprint 统一（关键！v2 修复）

- `hedge_core.py` 写入 result.json 的 fingerprint = **`期数_最新期号_特征版本`**（三要素，如 `8731_2026223_v6_110`）
- v2 起 `update.py` 的 `current_fingerprint()` 与之**完全一致**（v1 的 update_cloud.py 只返回两要素导致**永不匹配 → 数据未变也每次重算回测 → 每天 3 次 cron 白烧 Actions 分钟 + 每次提交部署**）
- pool.json 仍存两要素 + 顶层 feat_version，检查时拼接后比对

## 5. GitHub 仓库结构

```
fc3d-tens-kill/                    # GitHub 仓库根
├── index.html                     # 手机访问入口（GenSite 输出）
├── data/fc3d-history.csv          # 全量开奖（8730+ 期，云端自动追加）
├── cache/pool.json                # 专家池缓存（560条，提交以便免重算）
├── cache/result.json              # 回测/预测结果缓存（提交以便免重算）
├── engine.py / formulas.py        # 数据引擎 / 110特征+5924万公式库
├── bruteforce500.py               # 穷举选池
├── hedge_core.py                  # Hedge投票+网格扫描+回测+预测
├── gen_site.py                    # 生成页面（FC3D_OUT_HTML 环境变量控制文件名）
├── fetch.py                       # 多源降级抓取（跨源校验 + 单源兜底）
├── update.py                      # 一键更新单入口（--cloud / --force）
├── .github/workflows/update.yml   # 三重 cron + Pages 部署
└── README.md
```

## 6. 确定性保障（关键！）

**问题**：云端 runner 与本地 numpy 版本不同 → `np.argsort(-rates)` 对并列命中率的专家排序次序不同
（专家池 560 条命中率集中在 95~99%，并列极多）→ 选出的 TopK 专家不同 → 网格扫描最优参数漂移
（实测：本地 110/200 vs 云端 150/256）。

**修复**：`hedge_vote` 与 `build_leaderboard` 全部改用 `np.lexsort((索引, -命中率))` 稳定排序
（命中率降序、并列按索引升序），**跨环境/跨 numpy 版本结果完全一致**。

**原则**：
- 数据未变 → 复用已提交缓存，绝不重算（避免无谓漂移）
- 数据已变 → 稳定排序保证重算结果可复现、可审计

## 7. GitHub Actions 权限与部署

- `contents: write`：提交 CSV/缓存/页面
- `pages: write` + `id-token: write`：Pages 部署
- `actions/configure-pages` → `upload-pages-artifact`(path=./_site) → `deploy-pages`
- Pages 源：**GitHub Actions**（需在仓库 Settings→Pages 选择）
- `cancel-in-progress: true`：三重 cron 撞车取消旧运行，只保留最新
- **部署兜底（v2 修复）**：部署步骤**不设 changed 条件**，每次 cron 都重新部署当前 index.html。
  若某次部署失败（网络/Pages 瞬时故障），数据已提交、下次 cron 自动补部署——不会出现
  「数据已提交但页面永远卡旧」的静默故障
- **force_deploy 传递（v2 修复）**：手动 Run workflow 勾选 force_deploy=true 时，
  脚本以 `--force` 运行（v1 只强制部署旧页面，实际没重算）

## 8. 一致性保障

- 回测为逐期真实 walk-forward（第 t 期只用 t-1/t-2 数据）
- CSV 防倒灌：只追加严格晚于本地最新期号的期号
- 缓存指纹 = `期数_最新期号_特征版本`，不匹配自动重算
- 每轮 commit 前 `git diff --quiet` 检查 CSV/缓存/页面，无变化不提交（省 Actions 分钟）
- 本地与云端 7 个核心文件（engine/formulas/bruteforce500/hedge_core/fetch/gen_site/update）**字节级一致**，未来升级只需改本地 → 复制到 cloud_deploy → 推 GitHub

## 9. 手机查看

**浏览器直接访问**：
```
https://hxiaowei0102-web.github.io/fc3d-tens-kill/
```
可添加到主屏幕当 App 用。页面为移动优先自适应，红色大字显示下期十位杀码。

## 10. 已知风险与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| 所有数据源全部失效 | 低 | CSV 兜底 + 单源兜底(紧邻期采纳)；三重 cron 3 次机会 |
| 数据源给出脏数字 | 低 | 跨源交叉校验 ≥2 票 + 防倒灌校验 |
| Actions 免费额度(2000分/月) | 低 | 指纹缓存命中跳过重算；~24分/天(3次×~40s 无变化) |
| Pages 部署瞬时失败 | 低 | 部署步骤无条件，下次 cron 自动补部署 |
| 手动触发忘记传参 | 低 | 默认 false 正常跑；force_deploy 勾选才强制重算 |
| 算法命中率波动 | 统计 | 500期 99.80% / 1000期 95.00%，随机基线 90%，统计上限约 90%+ |
